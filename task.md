- `[x]` Setup & Feature Extraction
    - `[x]` Update `build_history.py` to map and extract `Total passes` (`sb_passes`) into `matches.parquet`.
    - `[x]` Run `build_history.py` to regenerate `data/built/matches.parquet`.
    - `[x]` Update `build_final_dataset.py` to compute raw tactical features (`Crosses_per_Game`, `PPDA`, `Fouls_Suffered_Ratio`), their rolling averages (l5 and l10), `has_boxscore_signal`, and perform robust competition/Elo imputation.
    - `[x]` Run `build_final_dataset.py` to generate the new `international_features_enriched_apifootball.csv`.
- `[x]` Signal Orthogonalization (Leakage-Free)
    - `[x]` Implement linear regression fitting on the training split inside the training/validation scripts.
    - `[x]` Save regression coefficients to `api/model_artifacts/style_ortho_weights.joblib`.
    - `[x]` Implement the residual calculation function to compute residuals on the fly for training, validation, and inference.
- `[x]` Cascade Architecture & Count Models
    - `[x]` Create `api/cards_gp_model.py` with `CardsGP` using the Generalized Poisson distribution, robust MLE, and fallback to standard Poisson.
    - `[x]` Update `api/predictor.py` to predict shots first, then inject shots predictions into corners/cards, and load `CardsGP`.
- `[x]` Validation, Backtesting & Verification
    - `[x]` Update `value_backtest.py` (and any other evaluation scripts) to compute Tail ECE and test tail lines.
    - `[x]` Re-train the shots, corners, and cards models using the new features.
    - `[x]` Verify Log-Loss and ECE metrics to ensure no regression and improved calibration.
    - `[x]` Save and present the experimental logs in `LOG_EXPERIMENTO_PROPS.md` and `walkthrough.md`.
